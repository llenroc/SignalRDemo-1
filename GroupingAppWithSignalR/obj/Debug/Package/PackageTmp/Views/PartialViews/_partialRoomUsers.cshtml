@model List<GroupingAppWithSignalR.Models.RoomUser>
@{
    Layout = null;
}
    
@{
    if (Model.Any())
    {
        foreach (var user in Model)
        {
            <li><img src="@user.Avatar" width="40"/><br /> @user.UserName</li>
        }
    }

}